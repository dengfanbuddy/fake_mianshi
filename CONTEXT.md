# CONTEXT.md — 领域术语表

> 本文件仅记录已确认的领域概念定义，不包含实现细节。

## 面试会话 (Interview Session)

一次完整的面试练习流程。包含两个可选阶段：**笔试**和**模拟面试**，按 笔试→模拟面试 顺序进行，用户可跳过任一阶段。两个阶段也可独立发起。模拟面试阶段可引用同次会话中的笔试结果进行追问。

面试题以**目标职位的技术内容**为主，包含少量行为/HR类问题（如离职原因等），目标是尽可能模拟真实面试。

## 笔试 (Written Test)

面试会话的第一阶段（可选）。以书面形式作答的技术考核环节。**限时制**：用户可配置总时长（默认 60 分钟），到时间自动提交。

题型包括：**单选题、多选题、填空题、简答题**。不包含手写代码题。系统设计题归入模拟面试阶段。

## 模拟面试 (Mock Interview)

面试会话的第二阶段（可选）。以对话形式进行的面试模拟，包含语音输入、AI 面试官实时互动。**不限时**，由面试官根据题量自然收尾总结。可引用同次会话的笔试结果进行追问。

面试问题**动态生成**：预设题单大纲（覆盖技术点 + 项目深挖 + 少量行为题），具体问题和追问根据用户回答实时生成，模拟真实面试官的应变能力。

## AI 模型配置 (AI Model Configuration)

系统预配置大语言模型 API 接口（如 DeepSeek 等），用于简历分析、出题、面试对话、面试分析等所有 AI 驱动环节。模型接口可配置切换。

## 简历分析 (Resume Analysis)

用户上传简历文件后，系统自动解析提取关键信息，包括但不限于：经验年限、职位历史、使用的技术栈、项目经历等。分析结果用于**定位目标面试岗位**，并向用户确认后进入后续流程。

简历文件格式：**PDF**。

## 职位需求 (Position Requirement)

出题的核心输入之一。有两种获取路径：

- **简历驱动**：上传简历 → 系统分析后建议目标岗位 → 用户确认
- **手动描述**：无简历时，用户按系统提供的格式提示自行描述目标岗位，包括职位名称、经验、资历级别、公司类型、具体技术、职位描述等

## 面试官风格 (Interviewer Persona)

模拟面试中 AI 面试官的人设风格。影响对话措辞、追问策略、以及 TTS 语音表现（语气、语速等）。

- **预设 + 自定义**：系统预设若干风格模板，用户可手动新增自定义风格
- **选择方式**：手动选择 / 随机分配 / 智能推荐（用户不选时默认推荐）
- **动态换人**：支持面试中途切换面试官风格，模拟多轮面试场景
- 一个面试官风格贯穿一段对话，切换后由新风格接续

## 语音交互 (Voice Interaction)

模拟面试中的语音输入输出机制。

- **输入模式**：MVP 阶段采用**录音后发送**模式（按住录音→停止→STT 转文字→AI 处理），后续迭代支持实时流式对话
- **输出模式**：面试官回复通过 **TTS 语音播报**，语音表现受面试官风格影响（语气、语速等）
- **STT/TTS 服务**：使用**腾讯云**语音服务（ASR 语音识别 + TTS 语音合成）
- **回答质量分析**：
  - **流畅度**：基于 STT 文本推断（停顿、语气词频率、语速、重复等）
  - **语气/情绪**：MVP 阶段基于文本内容由 LLM 推断（用词、句式等），后续可扩展音频信号分析增强
- **录音保存**：用户语音录音**保存**，用于回放复盘。音频文件存储于文件系统，数据库仅存路径引用。

## 面试分析 (Interview Analysis)

面试结束后生成的分析报告，分三个层次：

**单题分析（Per-Question）**：
- 笔试：对错判定 + 考察知识点 + 错因分析
- 模拟面试：回答准确度、深度、结构清晰度、流畅度、语气评估、具体改进建议

**单次会话分析（Session）**：
- 总体评分
- 优势项 / 弱项项
- 知识盲区汇总
- 沟通表达能力评估（仅模拟面试）
- 改进方案：薄弱知识点 + 推荐学习方向 + 下次重点建议

**历史综合分析（Historical）**：
- 跨多次面试的能力趋势（进步 / 停滞领域）
- 反复出错的高频弱点知识点
- 推荐下次重点练习方向

分析在面试结束后一次性生成，不在过程中实时评分，避免打断面试节奏。笔试偏客观评分，模拟面试偏主观评估。

## 面试项目 (Interview Project)

按目标岗位分组管理面试记录的顶层容器。每个项目绑定：简历 + 职位需求 + 面试历史记录 + 历史分析。用户可为不同目标岗位创建不同项目，各自独立追踪进展。

## 自适应出题 (Adaptive Question Generation)

基于历史面试分析智能生成新题的机制。采用**混合模式**：

- 系统从历史面试分析中提取**弱点标签**（如 "JVM调优-弱""并发编程-弱""Redis-已掌握" 等结构化标签）
- LLM 出题时接收：弱点标签 + 简历信息 + 职位需求，优先覆盖未掌握/常错的知识点，并可对简历项目进行深挖
- 每次面试结束后**自动更新**历史分析与弱点标签
- 用户可随时查看跨次面试的历史综合分析
