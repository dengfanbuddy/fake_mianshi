# 2026-08-01 工作日志

## Grill With Docs 会话完成

完成项目设计的 grill with docs 全流程，产出以下文件：

- **CONTEXT.md** — 领域术语表，定义了 11 个核心概念（面试会话、笔试、模拟面试、简历分析、职位需求、面试官风格、语音交互、面试分析、面试项目、自适应出题、AI 模型配置）
- **docs/adr/0001-tech-stack.md** — 技术栈 ADR：Spring Boot + Vue3 + SQLite
- **docs/adr/0002-adaptive-question-generation.md** — 自适应出题 ADR：混合模式（弱点标签 + LLM）
- **docs/adr/0003-voice-interaction.md** — 语音交互 ADR：录音后发送 + 腾讯云 ASR/TTS
- **docs/grilling-summary.md** — 完整总结

### 关键决策摘要
- 项目定位：个人面试训练工具，同时作为 Java 简历项目
- 技术栈：Vue3 + Spring Boot + SQLite + DeepSeek API + 腾讯云语音
- 笔试：单选/多选/填空/简答，限时，不手写代码
- 模拟面试：动态生成，不限时，语音对话，录音保存
- 面试官风格：预设+自定义，支持中途换人，影响 TTS
- 自适应出题：弱点标签 + LLM 混合模式
- 语音交互：MVP 录音后发送，后续迭代实时流式

## 实现计划编写完成

使用 writing-plans 技能编写完整实现计划，保存至 `docs/plans/2026-08-01-mock-interview-system.md`。

计划包含 12 个 Phase、24 个 Task：
- Phase 1: 项目骨架与基础设施（Spring Boot 初始化、Vue3 初始化、CORS/异常处理、JPA 实体、Repository）
- Phase 2: AI 模型集成（LLM Service、AI 配置 CRUD）
- Phase 3: 简历模块（PDF 解析、LLM 分析）
- Phase 4: 项目管理与职位需求
- Phase 5: 出题引擎（笔试题生成、面试大纲）
- Phase 6: 笔试模块（会话管理、前端 UI）
- Phase 7: 面试官风格（CRUD + 5 种预设）
- Phase 8: 模拟面试（动态对话生成）
- Phase 9: 语音集成（腾讯云 STT/TTS、音频录制存储）
- Phase 10: 面试分析（单题/会话/历史/弱点标签）
- Phase 11: 前端整合与路由
- Phase 12: 收尾（种子数据、端到端验证）

环境确认：Java 17 (GraalVM) 可用，Maven 损坏但可用 mvnw 绕过，Node v22 + npm 10 可用。

## 全量实现完成（子代理驱动）

用 subagent-driven-development 全流程执行 24 个 Task，17 个 commit，工作树干净。

**产出规模**：后端 85 个 Java 文件 + 14 个测试文件（92 个测试全绿），前端 32 个文件。

**关键实现**：
- Spring Boot 4.1.0（注意：2026 年 Spring Initializr 只支持 4.x；starter 名改为 spring-boot-starter-webmvc；Jackson 3.x 包名 tools.jackson.*；@MockitoBean 替代 @MockBean；AutoConfigureMockMvc 移到 org.springframework.boot.webmvc.test.autoconfigure；MockMvc 不自动带 context-path）
- SQLite + hibernate-community-dialects（org.hibernate.community.dialect.SQLiteDialect，Hibernate 7.4.1 实测可用）
- LLM 服务用 java.net.http.HttpClient + DeepSeek API；腾讯云 STT/TTS 手写 TC3 签名（有金标测试验证）
- 出题引擎 JSON 容错（JsonExtractor：去围栏+截括号分层降级）
- 模拟面试动态对话：大纲内存缓存 ConcurrentHashMap、笔试错题注入上下文、switchPersona 用换人消息记录
- 分析服务：事务提交后 afterCommit 触发避免 rollback-only 陷阱
- 前端：Vue3 + Element Plus，笔试/模拟面试/分析报告/项目详情/设置全页面；音频 webm→wav 转换（blobToWav 16kHz PCM）

**环境注意**：
- 本沙箱有 SERVER__PORT=0 环境变量 → Spring Boot 随机端口（E2E 时是 12168），用户真实环境用 8080
- 前端 npm run build 会被沙箱批量删除保护拦截（dist 清理），子代理用 --outDir 绕过
- 系统 Maven 损坏，必须用 backend/mvnw.cmd

**待用户完成**：Settings 页配置 DeepSeek API Key + 后端环境变量配腾讯云密钥，即可全流程使用。

## Bug 修复：ApiResponse code 不一致（23:30）

用户反馈设置页保存 DeepSeek 配置失败（红色 success 提示、刷新后列表为空）。

**根因**：后端 ApiResponse.success() 返回 code=0（成功），前端所有页面（30+ 处）判断 `res.code === 200`。配置实际已保存成功（DB 验证：id=2 deepseek-v4-flash is_active=1），前端误判失败。

**修复**：frontend/src/api/request.js 拦截器把 `code===0` 归一化为 `200`，一处修复全部页面。Vite HMR 即时生效，无需重启后端。

**遗留提醒**：用户配置的 apiUrl 是 https://api.deepseek.com/anthropic（Anthropic 兼容端点），但 LlmServiceImpl 发 OpenAI 格式请求，组合不兼容。需提醒用户改用 OpenAI 兼容端点 /v1/chat/completions。

## 补充：AI 配置编辑功能（23:30+）

用户反馈设置页没有"编辑"AI 配置的入口，无法修改已保存的配置（如 apiUrl/apiKey）。

**改动**：
- frontend Settings.vue：AI 配置表格操作列加"编辑"按钮（openEditConfig 预填表单，提交时带 id 走 JPA save 更新语义），弹窗标题区分新增/编辑
- backend AIModelConfigServiceImpl.save()：编辑（带 id）时保留原 createdAt，避免 JPA merge 把 created_at 覆盖为 null
- 新增测试：save_shouldPreserveCreatedAt_whenEditingExistingConfig（已通过）

**需要用户重启后端**才能生效（Java 改动），前端 Vite HMR 自动生效。
